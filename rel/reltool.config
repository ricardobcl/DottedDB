{sys, [
       {lib_dirs, ["../apps/", "../deps/"]},
       {rel, "dotted_db", "1",
        [
         kernel,
         stdlib,
         sasl,
         dotted_db
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "dotted_db"},
       {profile, embedded},
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},
       {excl_archive_filters, [".*"]},
       {app, sasl, [{incl_cond, include}]},
       {app, dotted_db, [{incl_cond, include}]}
      ]}.

{target_dir, "dotted_db"}.

{overlay_vars, "vars.config"}.

{overlay, [
           {mkdir, "data/ring"},
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {template, "files/app.config", "etc/app.config"},
           {template, "files/vm.args", "etc/vm.args"},
           {template, "files/dotted_db", "bin/dotted_db"},
           {template, "files/dotted_db-admin", "bin/dotted_db-admin"}
           ]}.
